services:
  db:
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres@todo
      POSTGRES_DB: todo
    volumes:
      - ./dev/data:/var/lib/postgresql/data


